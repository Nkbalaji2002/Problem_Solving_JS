// write the function to check if a number is even or odd
const EvenorOdd = (number) => {
  if (number % 2 === 0) {
    console.log(`${number} is Even Number`);
  } else {
    console.log(`${number} is Odd Number`);
  }
};

EvenorOdd(20);
EvenorOdd(27);
